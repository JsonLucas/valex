import axiosInstance from "..";
import { Login, ISignUser } from "../../types/users";

export const signUpRequest = async (body: ISignUser) => {}

export const signInRequest = async (body: Login) => {}